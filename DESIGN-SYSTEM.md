# PÉONIA Studio — дизайн-система

Зібрана з восьми референсів із папки `PÉONIA Studio` і покладена на палітру бренду.
Референси: 1 — PRIMAL (маскований reveal), 2–5 — Green Island (флористика),
6–8 — скінкер-бренд (три колонки, текст із проколом, тип на фото).

---

## 1. Що спільного у восьми референсах

### Типографіка — «два голоси»

У всіх восьми працює **одна й та сама пара**: нейтральний геометричний гротеск + каліграфічний
скрипт. Третього шрифту немає ніде.

- **Гротеск** — світлого накреслення, у заголовках великими літерами, з ледь від'ємним трекінгом і
  щільним інтерліньяжем (реф. 3, 5, 6, 7, 8). Тіло тексту — той самий гротеск, часто теж капсом,
  дрібно.
- **Скрипт** — тільки як акцент, ніколи як масив. Два способи вживання:
  - **підміна однієї літери в слові**: `W`ORK, `B`E, `O`UR, FLO`W`ERS (реф. 2–5) — скрипт стоїть
    на місці першої літери, більший за капс і звисає над рядком;
  - **ціле слово**: *skincare*, *ingredient*, *daily care*, *body & hand* (реф. 6–8).
- Акцентне слово часто ще й **змінює колір** (реф. 6: лаймовий серед чорного).
- **Мікролейбли** — крихітний капс із широким трекінгом, іноді з номером: `01/ STREET CULTURE`.
- **Цифри як графіка** — величезні числа з малесеньким підписом у дужках верхнім індексом:
  `100%` `(Of flowers)` (реф. 5).

### Сітка й простір

Спільний принцип — **текст і зображення зчеплені, а не поставлені поруч**:

- квітка лежить **між рядками** заголовка, перекриваючи одні літери й ховаючись за іншими (реф. 2);
- піпетка **проколює** текстовий блок посередині, рядки обтікають її зліва й справа (реф. 6);
- назва бренду **заходить за** обличчя персонажа (реф. 1);
- шрифт лежить **на фото** руки (реф. 8).

Далі — асиметрія й різноформатність:

- фото різного розміру зі **зсувом по вертикалі**, у стрічку з горизонтальним драгом (реф. 4);
- **пара «велике + мале»**, підпис під меншим, шириною рівно в нього (реф. 5);
- **три повновисотні панелі** впритул, без проміжків, кожна оформлена інакше: суцільний колір,
  фото на весь виліт, білий типографічний блок (реф. 7);
- **послідовна стежка** карток, з'єднаних пунктиром (реф. 3).

Простір роздають щедро: заголовок сидить у порожнечі, дрібний текст ставлять малими острівцями
збоку, а не одним стовпцем. Вертикальні відступи між блоками більші за будь-який відступ усередині.

### Палітра

Скрізь однакова конструкція з трьох ролей:

| Роль | Green Island | Скінкер | Частка екрана |
|---|---|---|---|
| Тло | вершкове / блідо-шавлієве | біле / вершкове | ~80% |
| Текст | глибокий зелений | чорний | ~15% |
| Акцент | бордова крапка CTA | лайм | ~5% |

Колір у макеті майже не працює — **насиченість дають самі фотографії**. Жодних градієнтів,
тіней і об'єму в інтерфейсі: усе плоске, межі — волосяні лінії.

### Ритм блоків

Секції чергуються за типом подачі, ніколи два однакові поспіль:
**типографічний → зображення → дані → панелі → зображення**. У кожній секції рівно один
фокус — два конкуруючі акценти не трапляються ніде.

---

## 2. Перенесення на PÉONIA

Ролі з референсів переносимо один в один: вершкове тло → **світло-зелений папір**,
глибокий зелений → **той самий глибокий зелений**, лайм → **кораловий рожевий**.
Насиченість, як і в референсах, дають самі квіти.

> Це друга редакція палітри. Перша (бордовий/пудровий/молочний) виявилася надто тьмяною —
> замінена на зелень із коралом за прямим запитом. Типографічна пара не змінилася.

---

## 3. Токени

```css
:root {
  /* ——— ґрунти: світлий крем, як у реф. 2 ——— */
  --paper:     #FBF9F0;   /* основне тло сторінки */
  --paper-2:   #F5F3E6;   /* ледь відчутне чергування */
  --paper-3:   #EDEBDC;   /* глибше чергування, футер */
  --cream:     #FFFEF8;   /* картки й панелі */

  /* ——— чорнило ——— */
  --green:      #00623B;  /* заголовки, темні панелі   6.5:1 на paper */
  --green-deep: #004529;  /* основний текст            9.7:1 на paper */
  --green-mute: #1A6A4A;  /* мікролейбли, мета         4.6:1 навіть на paper-3 */

  /* ——— акцент ——— */
  --coral:        #FF9EA9; /* тільки на зеленому тлі або як декор */
  --coral-ink:    #D37480; /* скрипт-акцент на світлому, від 32px */
  --coral-ink-sm: #B35562; /* акцент дрібним кеглем на світлому */

  /* ——— лінії ——— */
  --line:        #D7E3D6;
  --line-strong: #B9D1C0;
}
```

### Контрастні правила (перевірені, не на око)

- `--coral` `#FF9EA9` на кремовому дає **1.9:1** — як текст **заборонений**.
  Працює лише як декор: круглі плашки, лінії, заливки.
- Скрипт-акцент на світлому ґрунті — `--coral-ink`, і **тільки від 32px**.
  Дрібніше — `--coral-ink-sm`.
- Мікролейбли — `--green-mute`: єдиний приглушений зелений, що тримає 4.5:1
  на всіх трьох ґрунтах, включно з найтемнішим `--paper-3`.

## 4. Типографічна шкала

| Роль | Розмір | Накреслення | Трекінг | Інтерліньяж |
|---|---|---|---|---|
| Hero-заголовок | `clamp(3rem, 9vw, 8.5rem)` | 300, CAPS | `-.02em` | `.92` |
| Заголовок секції | `clamp(2.2rem, 5.5vw, 4.75rem)` | 300, CAPS | `-.015em` | `.96` |
| Підзаголовок | `clamp(1.4rem, 2.6vw, 2.1rem)` | 300 | `-.01em` | `1.15` |
| Лід | `clamp(1.05rem, 1.5vw, 1.3rem)` | 300 | `0` | `1.55` |
| Текст | `1rem` | 300 | `.005em` | `1.7` |
| Текст капсом (як у реф. 7–8) | `.875rem` | 400, CAPS | `.06em` | `1.55` |
| Мікролейбл | `.6875rem` | 500, CAPS | `.22em` | `1.2` |
| Велика цифра | `clamp(2.8rem, 7vw, 6rem)` | 300, `tabular-nums` | `-.03em` | `1` |
| Підпис цифри | `.6875rem` | 400, у дужках | `.08em` | `1.2` |

### Скрипт: правила вживання

1. **Максимум один акцент на заголовок.** Два скрипти в одному блоці — вже не акцент.
2. Кегль скрипта — **1.25–1.4× від капсу поруч**: у скрипта низький зріст рядкових, інакше він
   провалюється. Плюс `line-height: 0` і ручний `translateY`, щоб не розпирало рядок.
3. Ніколи не капсом, ніколи не в тілі тексту, ніколи в кнопках, лейблах, навігації, формі.
4. Дві дозволені форми:
   - **підміна першої літери**: `<span class="swash">B</span>OUQUET`;
   - **ціле слово в реченні**: `у кожному <span class="swash-word">Strauß</span>`.
5. Обов'язково `aria-hidden` не ставити — літера має лишатися в тексті для читалок; змінюється
   тільки накреслення, не вміст.
6. Український і німецький скрипт повинні мати ті самі гліфи — Pinyon Script покриває латиницю,
   але **не кирилицю**. Для української версії акцент падає на `Snell Roundhand`/системний
   курсив або (краще) акцентується не скриптом, а кольором + курсивом гротеска. Це закладено у
   фолбек `--script`.

---

## 5. Патерни блоків

Вісім патернів, по одному з кожного референса. Кожна секція лендингу бере рівно один.

**P1 «Завіса» (реф. 1).** Стартовий екран: суцільний темно-коричневий шар, який рваною
органічною маскою «змивається» й відкриває фото піонів із вордмарком. Маска — SVG-форма з
нерівним краєм, що масштабується від 0 до 1.6 за ~1.1 с. Вордмарк частково заходить **за** суцвіття.
Показувати один раз за сесію (`sessionStorage`), повністю вимикати при `prefers-reduced-motion`.

**P2 «Квітка в тексті» (реф. 2).** Заголовок у два-три рядки, між рядками — вирізане суцвіття, яке
перекриває частину літер і ховається за іншими (`z-index` по шарах). У виріз заголовка вбудована
кругла бордова CTA-плашка з мікролейблом. Два невеликі острівці тексту по 30–40 знаків у рядку,
поставлені асиметрично.

**P3 «Стежка» (реф. 3).** *Знято з сайту — залишено в системі як довідка.* Пронумеровані картки, розкидані сходинкою вгору-вниз і з'єднані
пунктирною кривою (`stroke-dasharray` на SVG-path). Кожна картка: іконка + заголовок + короткий
текст. На вузькому екрані стежка випрямляється у вертикальний список, пунктир лишається.

**P9 «Колода» (реф. ORA).** Каталог сортів: ряд перекритих карток із заокругленими кутами,
кожна ледь повернута, кольорова пігулка з назвою сорту лежить поверх краю картки. Клік по
картці розкриває під рядом опис сорту; повторний клік згортає. На вузькому екрані колода
розкладається в сітку без поворотів. CTA — під картками.

**P4 «Розсип» (реф. 4).** Горизонтальна стрічка фото різних пропорцій зі зсувом по вертикалі,
гортається драгом; курсор перетворюється на круглу плашку `DRAG`. Фолбек — звичайний
`overflow-x: auto` зі `scroll-snap`.

**P5 «Пара + цифри» (реф. 5).** Велике фото (7 колонок) і мале (4 колонки) у ряд, вирівняні по
верху. Текст стоїть **під меншим фото і шириною рівно в нього**. Нижче — ряд із чотирьох цифр,
розділених волосяними вертикалями, з підписами в дужках верхнім індексом.

**P6 «Прокол» (реф. 6).** Центрований текстовий блок, крізь який вертикально проходить вузьке
зображення; рядки розступаються ліворуч і праворуч від нього. Одне-два слова — скриптом і
акцентним кольором. Навколо блоку — щонайменше `--section-lg` порожнечі.

**P7 «Триптих» (реф. 7).** Три повновисотні панелі впритул, **без проміжків**, кожна оформлена
інакше: (1) суцільний колір + дрібний об'єкт + пігулка-кнопка, (2) фото на весь виліт без тексту,
(3) світлий типографічний блок зі скриптом і капсовим текстом. На мобільному — три екрани поспіль.

**P8 «Тип на кольорі» (реф. 8).** Початково — типографіка просто на фотографії. Оскільки всі
квіти тепер вирізані на прозорому тлі, фотофонів на сайті немає, і патерн працює інакше:
суцільний зелений ґрунт, вершковий текст і велике вирізане суцвіття, що заходить за край
секції. Логіка збережена — тип і зображення в одному шарі, без підкладок під текст.

---

## 6. Ритм сторінки

Ґрунти чергуються, два однакові поспіль — заборонено:

```
Hero            P1  фото на весь екран
Переваги        P3  --milk
Каталог сортів  P9  --paper-2
Приватним/B2B   P7  панелі впритул
Філософія       P6  --milk-warm
B2B-умови       P5  --burgundy (темна панель)
Галерея         P4  --paper-2 (фото у рамках, не вирізи)
Відгуки         P2  --blush-tint
Про нас         P5  --milk
Фінальний CTA   P8  фото + тип
```

---

## 7. Рух

- Поява блоків: `opacity 0→1`, `translateY 24px→0`, 700 мс, `cubic-bezier(.22,.61,.36,1)`,
  сходинка між дочірніми 80 мс.
- Фото в кадрі: повільний `scale(1.06→1)` за 1.4 с.
- Ховер на картці сорту: зображення `scale(1.04)`, підпис зсувається на 4px, 400 мс.
- Кнопка: заливка розкривається знизу вгору, 300 мс.
- **Усе перелічене вимикається** під `@media (prefers-reduced-motion: reduce)`, включно з P1.

---

## 7a. Зображення: тільки вирізи

Усі квіти — **вирізані на прозорому тлі**, жодних прямокутних кадрів і жодних підкладок.

Вирізи робить нейромережева модель матінгу (`rembg`, u2net / isnet-general-use) з
alpha-matting на краях. До неї була евристика за кольором — вона не впоралася: щоб прибрати
листя, зрізане межею кадру, доводилося обмежувати виріз еліпсом, і той еліпс читався як
**рамка навколо квітки**. Це головна помилка, якої не можна повторювати.

Правила:

- **Квітка ціла.** Суцвіття, зрізане краєм кадру, не використовуємо взагалі.
- **Ніяких геометричних масок.** Ні еліпсів, ні прямокутників, ні кругів — контур має
  йти по пелюстках.
- **Листя і стебло — вітаються.** Вони дають відчуття, що квітка росте, а не лежить.
  Найкраще працюють знімки, де рослина зі стеблом і листям знята на рівній стіні: там
  модель тримає весь кущик одним об'єктом (`plant-*.webp`).
- **Тінь замість підкладки.** `drop-shadow` з зеленим відтінком, щоб виріз не висів у повітрі.

## 8. Чого не робимо

- Жодних тіней, градієнтів і об'єму в інтерфейсі — площина й волосяні лінії.
- Заокруглення тільки у пігулок-кнопок і круглої CTA-плашки; картки й фото — прямий кут.
- Ніякого суцільного затемнення поверх фотографій.
- Кораловий ніколи не несе текст на світлому тлі.
- Скрипт ніколи не з'являється у навігації, кнопках, формі, лейблах.
- Два акценти в одній секції — ні: один фокус на блок.
- Іконки — тільки волосяний контур в один вагу, без заливок.

---

## 9. Шрифти

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Jost:wght@300;400;500&family=Pinyon+Script&display=swap" rel="stylesheet">
```

Jost покриває латиницю й кирилицю — придатний для обох мовних версій.
Pinyon Script — **тільки латиниця**; в українській версії акцент виконується курсивом Jost плюс
кольором (див. §4, п. 6).
